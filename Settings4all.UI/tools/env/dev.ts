import { EnvConfig } from './env-config.interface';

const DevConfig: EnvConfig = {
  ENV: 'DEV',
  SETTINGS4NET_API: 'http://localhost/settings4net.API/api/',
  APP_BASE: 'ui'
};

export = DevConfig;
